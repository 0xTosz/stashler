# Screenshots

These are **placeholder** images so the README renders without broken links. Replace each
PNG with a real capture (same filename) — the README references them automatically.

Suggested shots (UI at `http://127.0.0.1:7137`, with some items captured):

| file | capture |
|------|---------|
| `hero.png` | Records page with a row expanded into its item card (the money shot) |
| `records.png` | Records table: filters + sortable columns, ideally an expanded card |
| `queue.png` | Review Queue: a few flagged cards showing the per-match tiles |
| `settings.png` | Settings: credentials + league dropdown + the rules/filter editor |
| `evaluation.png` | The rules editor (or a flagged item showing its match tiles) |

Tip: capture at a ~1280px-wide window for a consistent look on GitHub.
