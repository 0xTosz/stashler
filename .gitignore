# Secrets / local data — POESESSID lives in the DB/config, never commit it
*.db
*.db-wal
*.db-shm
config.local.toml
rules.local.toml
.env

# Packaging artifacts
stashler.ico
*.spec.bak

# Archetype sets — the user's working copy + restore-point + upgrade backup live in the data
# dir (the dev cwd); the packaged default (stasher/evaluate/default_archetype_set.yaml) IS tracked.
*.generated.yaml
archetype_set.yaml
archetype_set.default.yaml
archetype_set.bak.yaml

# Local scratch: loot filters, API/page dumps, screenshots
FilterBlade/
*.filter
ninja_builds.html
ninja_doc.md
resp.json
stashler.png
/None

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
